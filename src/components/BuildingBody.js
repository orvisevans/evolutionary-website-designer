import React, { PropTypes } from 'react';
import { Link, IndexLink } from 'react-router';

const BuildingBody = (props) => {
  return (
    <div>
      <h3>BuildingBody</h3>
    </div>
  );
};

export default BuildingBody;
