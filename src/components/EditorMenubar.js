import React, { PropTypes } from 'react';
import { Link, IndexLink } from 'react-router';

const EditorMenubar = (props) => {
  return (
    <div>
      <p>EditorMenubar</p>
      <button>Swap Headers</button>
    </div>
  );
};

export default EditorMenubar;
